<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR Code for <%= stock_id %></title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* This hides the nav, header, and buttons when printing */
        @media print {
            body {
                padding: 0;
                margin: 0;
            }
            .no-print {
                display: none !important;
            }
            main {
                padding: 0 !important;
                max-width: 100% !important;
            }
            .print-only-card {
                box-shadow: none !important;
                border: none !important;
                margin: 0;
                padding: 0;
                width: 100%;
                max-width: 100%;
            }
            .print-only-qr {
                width: 80%;
                max-width: 400px;
                margin: 0 auto;
            }
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen">

    <!-- Admin Navigation Bar (Hidden on Print) -->
    <nav class="bg-white shadow-md no-print">
        <div class="container mx-auto px-4 max-w-7xl">
            <div class="flex justify-between items-center h-16">
                <!-- Logo/Title -->
                <div class="flex-shrink-0 flex items-center">
                    <h1 class="text-xl font-bold text-gray-900">Diamond CMS</h1>
                </div>
                <!-- Navigation Links (with permission checks) -->
                <div class="hidden md:flex md:space-x-8">
                    <a href="/admin" class="text-gray-500 hover:text-gray-900 border-b-2 border-transparent hover:border-gray-300 font-medium px-1 py-4">Dashboard</a>
                    <a href="/admin/manage" class="text-blue-600 border-b-2 border-blue-600 font-medium px-1 py-4">Manage Stones</a>

                    <% if (user.is_superadmin) { %>
                        <a href="/admin/users" class="text-gray-500 hover:text-gray-900 border-b-2 border-transparent hover:border-gray-300 font-medium px-1 py-4">Manage Users</a>
                    <% } %>

                    <a href="/admin/history" class="text-gray-500 hover:text-gray-900 border-b-2 border-transparent hover:border-gray-300 font-medium px-1 py-4">History</a>

                    <% if (user.is_superadmin || user.can_view_analytics) { %>
                        <a href="/admin/analytics" class="text-gray-500 hover:text-gray-900 border-b-2 border-transparent hover:border-gray-300 font-medium px-1 py-4">Analytics</a>
                    <% } %>
                </div>
                <!-- User/Logout -->
                <div class="flex items-center">
                    <span class="text-gray-500 mr-4">Welcome, <%= user.username %>!</span>
                    <a href="/logout" class="text-sm text-blue-600 hover:underline">Log Out</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Page Header (Hidden on Print) -->
    <header class="bg-white shadow-sm no-print">
        <div class="container mx-auto px-4 py-4 max-w-7xl flex items-center justify-between">
            <h2 class="text-2xl font-bold text-gray-900">QR Code & Share Links</h2>
            <a href="/admin/manage" class="text-sm text-blue-600 hover:underline">&larr; Back to Manage Stones</a>
        </div>
    </header>

    <main class="container mx-auto px-4 py-8 max-w-xl">

        <!-- QR Code Card -->
        <div class="bg-white p-8 rounded-lg shadow-lg text-center print-only-card">
            <h3 class="text-3xl font-bold mb-4">Stock ID: <%= stock_id %></h3>

            <!-- 
              UPDATED: The 'publicUrl' passed from index.js now
              includes the utm_source=qrcode tag automatically!
            -->
            <div class="w-full max-w-xs mx-auto my-6 print-only-qr">
                <img src="<%= qrCodeDataUrl %>" alt="QR Code for <%= stock_id %>" class="w-full h-auto border-4 border-gray-300 rounded-lg">
            </div>

            <p class="text-gray-500 text-sm mb-6 break-words">
                Scans to (with tracking): 
                <a href="<%= publicUrl %>" target="_blank" class="text-blue-600 hover:underline"><%= publicUrl %></a>
            </p>

            <!-- Action Buttons (Hidden on Print) -->
            <div class="flex flex-col sm:flex-row gap-4 no-print">
                <!-- Download Button -->
                <a href="<%= qrCodeDataUrl %>" 
                   download="qr_<%= stock_id %>.png"
                   class="flex-1 block text-center bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg text-lg transition duration-200">
                   Download
                </a>

                <!-- Print Button -->
                <button onclick="window.print()"
                   class="flex-1 block text-center bg-gray-600 hover:bg-gray-700 text-white font-bold py-3 px-6 rounded-lg text-lg transition duration-200">
                   Print
                </button>
            </div>
        </div>

        <!-- NEW: UTM Link for other shares (e.g., WhatsApp) -->
        <div class="bg-white p-8 rounded-lg shadow-lg text-left mt-8 no-print">
            <h3 class="text-2xl font-bold mb-4">Custom Share Links</h3>
            <p class="text-gray-600 mb-4">
                Copy these links to automatically track your shares in the "Analytics" panel.
            </p>

            <div class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700">Link for WhatsApp:</label>
                    <input type="text" readonly value="<%= baseUrl %>/diamonds/<%= stock_id %>?utm_source=whatsapp&utm_medium=social" class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50 text-gray-600 focus:outline-none" onclick="this.select()">
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700">Link for Instagram:</label>
                    <input type="text" readonly value="<%= baseUrl %>/diamonds/<%= stock_id %>?utm_source=instagram&utm_medium=social" class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50 text-gray-600 focus:outline-none" onclick="this.select()">
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700">Link for Email Campaign:</label>
                    <input type="text" readonly value="<%= baseUrl %>/diamonds/<%= stock_id %>?utm_source=newsletter&utm_medium=email&utm_campaign=nov_sale" class="w-full px-3 py-2 border border-gray-300 rounded-lg bg-gray-50 text-gray-600 focus:outline-none" onclick="this.select()">
                </div>
            </div>
        </div>
    </main>

</body>
</html>